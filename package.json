{
  "name": "tess",
  "version": "1.0.0",
  "description": "A GUI for tesseract-ocr",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc && copyfiles -f src/index.html build && copyfiles -f src/*.css build",
    "start": "npm run compile && electron ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "copyfiles": "^2.4.1",
    "electron": "^18.2.0",
    "tesseract.js": "^3.0.2",
    "typescript": "^4.6.4"
  },
  "devDependencies": {
    "@electron-forge/cli": "^6.0.0-beta.63"
  }
}
